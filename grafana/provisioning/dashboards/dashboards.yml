apiVersion: 1

providers:
  - name: "pqc-dashboards"
    orgId: 1
    folder: "PQC Payment Migration"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
